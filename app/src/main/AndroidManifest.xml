<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".UploadResource"
            android:exported="false"
            android:label="UploadResource"/>
        <activity
            android:name=".NewFile"
            android:exported="false"
            android:label="NewFile" />
        <activity
            android:name=".NewFloder"
            android:exported="false"
            android:label="NewFloder" />
        <activity
            android:name=".new_prj"
            android:exported="false"
            android:label="@string/title_activity_new_prj"
            android:theme="@style/Theme.MyApplication.NoActionBar" />
        <activity
            android:name=".CreateNewPrj"
            android:exported="false"
            android:label="@string/title_activity_create_new_prj" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>